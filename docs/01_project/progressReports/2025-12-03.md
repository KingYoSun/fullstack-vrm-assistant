# 2025年12月03日 進捗レポート

## 概要
- docker compose を profiles ベースに統合し、dev/prod/mock の切り替えとホットリロード開発手順を単一ファイルで管理できるように整理。

## 成果
- `docker-compose.yml` に backend-dev/frontend-dev を dev プロファイルとして追加し、llm/stt/tts/embedding/postgres を prod/dev 両対応の profiles で起動できるよう統合（旧 `docker-compose.dev.yml` は廃止）。
- COMPOSE_PROFILES 前提の運用に合わせ、`.env.default`、Makefile（`make dev`/`make dev-all`）、AGENTS/README/実装ドキュメントの起動手順を更新。
- `COMPOSE_PROFILES=prod|dev docker compose config` で compose 構成を生成し、シンタックスエラーがないことを確認。

## 課題
- dev/prod/mock それぞれのプロファイルでモデル配置後の実起動・ヘルス確認は未実施。

## 次のアクション
- COMPOSE_PROFILES を切り替えて `docker compose up` を実行し、`/ready` や各プロバイダのヘルス確認ログを取得する。
- モデル配置後に `make dev-all` 相当でホットリロード開発動線を検証し、レイテンシ計測タスク（partial/tts_start p95）に着手する。
