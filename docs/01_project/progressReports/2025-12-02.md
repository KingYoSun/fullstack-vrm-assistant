# 2025年12月02日 進捗レポート

## 概要
- DGX Spark (aarch64/cu130/SM 12.1) 向けのモデル選定と docker compose 実装に合わせ、主要ドキュメントを更新。

## 成果
- `docs/design_doc.md` を現行構成に合わせて更新（NIM Qwen3-32B, whisper.cpp, Fish Speech/OpenVoice, llama.cpp embedding、GPU リソース指定とモック切替の例を反映）。
- `docs/03_implementation/production_runtime.md` を aarch64/CUDA13 前提で全面書き換え、各モデルの取得・配置手順と build/起動コマンド、ヘルス/レイテンシ確認手順を整理。
- `docs/03_implementation/mvp_plan.md` の実運用タスクとリスクを新プロバイダ構成に合わせて更新し、`docs/01_project/activeContext/summary.md` に最新ランタイム手順への導線を追加。

## 課題
- 実機でのモデル配置（Qwen3, whisper.cpp GGUF, Open Audio S1 mini, GGUF embedding）と `docker compose up` の起動確認・レイテンシ計測は未実施。

## 次のアクション
- `.env` に NGC_API_KEY/モデルパスをセットし、`docker compose build stt tts embedding` → `docker compose up` で実機起動を確認。
- `docs/01_project/tasks/status/in_progress.md` の計測項目に沿って partial/final/tts_start の p95 を採取し、証跡を保存。
