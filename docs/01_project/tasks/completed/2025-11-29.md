# 2025年11月29日

- dev backend コンテナの webrtcvad ビルド失敗を解消。
  - `docker-compose.dev.yml` の backend で `build-essential` を追加し、pip セットアップを `pip/setuptools/wheel` 事前導入 + `PIP_NO_BUILD_ISOLATION=1` に変更してビルド依存の不足/再取得を防止。
  - `docker compose -f docker-compose.dev.yml --profile dev up backend` で webrtcvad のビルド完了と uvicorn 起動を確認（ログに `/workspace/backend` のホットリロード待ちが出る状態）。
- `docs/03_implementation/mvp_plan.md` の「実運用レベルへの残タスク」を対応。
  - 本番用 `docker-compose.yml` を追加し、vLLM/sherpa-onnx/Open Audio S1/text-embeddings-inference をデフォルト構成、`profile=mock` を分離。GPU device_requests・モデルボリューム・ヘルスチェックを設定。
  - `config/providers.yaml` を環境変数化し、`.env` 系にパラメータを追加。未解決プレースホルダ検知を backend 側に入れて設定漏れを検知。
  - `docs/03_implementation/production_runtime.md` にプロバイダ起動手順、RAG ingest コマンド、ヘルス/フォールバック確認、運用ノートを追加。レイテンシ実測のみ未了として mvp_plan に残置。
