# 実装ガイド概要

- 開発環境は `docker-compose.dev.yml` と `Makefile` (`make dev`/`make dev-all`/`make dev-down`) を利用。初回は `cp env.example .env` を実行してから `make dev` で backend/frontend を起動。
- Backlog: dev 用 compose / lint・test コマンドの具体値を追記予定。
